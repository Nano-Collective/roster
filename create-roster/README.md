# create-roster

```bash
npm create roster my-org
```

Makes the directory, then runs [`@nanocollective/roster`](https://github.com/Nano-Collective/roster)
in it. With no org there yet, that opens the setup portal in your browser.

A shim, on purpose. Everything it could do, `roster` already does.
